name: Auto merge
on:
  pull_request:
    types: [opened, synchronize, reopened]

permissions:
  contents: write
  checks: read
  pull-requests: write

jobs:
  auto-merge:
    # ⭐ No actor filter - works for anyone
    uses: ./.github/workflows/sample.yml
    with:
      azure_cloud: true
      tfchecks_azure: '["pr-validation / 📝 Validate PR title", "pr-validation / 🧾 Validate Commit Messages", "tf-lint / tflint"]'
    secrets:
      GITHUB: ${{ secrets.GITHUB }}
